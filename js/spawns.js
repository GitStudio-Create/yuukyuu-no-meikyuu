(function(K){'use strict';function range(a,b){return a+K.Util.rand(b-a+1);}function limit(id){return id==='tutorialDungeon'?6:id==='normalDungeon'?10:14;}K.Spawns={enemyCount:function(id,f){if(id==='tutorialDungeon')return f===1?range(2,3):f<=3?range(3,4):range(4,5);if(id==='normalDungeon')return f<=5?range(3,5):f<=15?range(4,6):range(5,8);return f<=15?range(4,6):f<=40?range(5,8):range(7,10);},maxEnemies:limit,nextInterval:function(floor){var fastest=Math.max(25,38-Math.floor(floor/5));return range(fastest,40);},policy:function(id,floor,currentTurn){return{initialCount:this.enemyCount(id,floor),naturalSpawnInterval:this.nextInterval(floor),nextSpawnTurn:(currentTurn||0)+this.nextInterval(floor),maxEnemies:limit(id)};},findSpawnCell:function(state){for(var i=0;i<180;i++){var p=K.Map.freeCell(state,0);if(p&&K.Util.distance(p,state.player)>8&&!state.seen[K.Util.key(p.x,p.y)]&&K.Util.distance(p,state.stairs)>3)return p;}return null;},tryNaturalSpawn:function(state){var policy=state.spawnPolicy;if(!policy||state.turn<policy.nextSpawnTurn)return false;policy.nextSpawnTurn=state.turn+this.nextInterval(state.floor);policy.naturalSpawnInterval=policy.nextSpawnTurn-state.turn;if(state.enemies.length>=policy.maxEnemies)return false;var p=this.findSpawnCell(state);if(!p)return false;var def=K.EnemyCatalog.pickForState?K.EnemyCatalog.pickForState(state):K.EnemyCatalog.pick(state.dungeonId,state.floor);state.enemies.push(K.Entities.createEnemy(state.floor,p,K.Dungeons.get(state.dungeonId),def.id));K.State.addLog('遠くの回廊から、新たな気配が近づいた。');return true;}};})(window.Kiri=window.Kiri||{});
