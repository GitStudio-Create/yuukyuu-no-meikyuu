# 悠久の迷宮 引き継ぎメモ

このファイルは、長くなったCodexスレッドから新しいスレッドへ開発を引き継ぐためのメモです。
新しいスレッドでは、まず `AGENTS.md` とこの `HANDOFF.md` を読んでから作業してください。

## プロジェクト基本情報

- プロジェクト名：悠久の迷宮
- 旧コンセプト名：霧灯りの迷宮
- 種別：HTML / CSS / JavaScript / Canvas 製の軽量ターン制ローグライク
- ローカル作業場所：`C:\Users\Kenji\Documents\Codex\悠久の迷宮`
- GitHub：`https://github.com/GitStudio-Create/yuukyuu-no-meikyuu.git`
- 公開想定：静的ファイルとして GitHub Pages で動作
- 外部ライブラリ：基本なし
- 音源：`BGM/` 配下のMP3をプロジェクトに含めて管理

## 最初に必ず読むファイル

1. `AGENTS.md`
   - 作業ルール、テスト方針、禁止事項が書かれています。
   - 依頼範囲だけを最小限変更する方針です。
2. `HANDOFF.md`
   - この引き継ぎメモです。
3. 必要に応じて `CHANGELOG.md`
   - 直近の実装・修正履歴を確認できます。
4. 必要に応じて `TODO.md`
   - 現在確認済みの未実装・改善予定を確認できます。
5. 必要に応じて `KNOWN_ISSUES.md`
   - 現時点で確認済みの未解決不具合を確認できます。

## 作業運用ルール

- 現在のリポジトリのコード・ドキュメントを正としてください。
- 過去チャットの記憶より、現行ファイルを優先してください。
- 依頼された範囲以外のゲーム仕様、数値、UI、ファイルは変更しないでください。
- 大規模な全面リファクタリングは、明示依頼がある場合のみ行ってください。
- 長時間テスト、全Stageテスト、長時間マップ生成ストレステストは、明示依頼がない限り実施しないでください。
- 通常の修正では、変更箇所に関係する短時間テストだけで十分です。
- 機能追加、仕様変更、不具合修正を行った場合は、必要に応じて `CHANGELOG.md` を更新してください。
- 「悠久の迷宮」は試作品のため、ユーザーは確認なしのコミット・pushを許可しています。
  - ただし、破壊的操作や外部公開上のリスクがある場合は安全確認してください。

## 主要ファイル構成

- `index.html`
  - 画面構造と各JavaScriptファイルの読み込み。
  - CSS/JSのキャッシュ更新用クエリが付いている箇所があります。
- `style.css`
  - PC・スマホのレイアウト、ステータス欄、操作UI、道具袋など。
  - 末尾側にスマホ向けの強い上書きCSSが多いため、UI修正時は競合に注意してください。
- `js/game.js`
  - ゲーム進行の統括。
- `js/state.js`
  - ゲーム状態とセーブ関連。
- `js/dungeons.js`
  - 迷宮種別、階層ルール。
- `js/map.js`
  - マップ生成、部屋・通路、配置候補。
- `js/entities.js`
  - プレイヤー・敵の行動。
- `js/items.js`
  - アイテム定義。
- `js/item-actions.js`
  - 使用、装備、射撃、投擲、設置などの道具処理。
- `js/item-details.js`
  - アイテム説明と識別表示。
- `js/inventory.js`
  - 道具袋、手動整理、装備優先。
- `js/input.js`
  - キーボード・スマホ入力。
- `js/ui.js`
  - Canvas描画と画面表示。
- `js/stage11-ui.js`
  - ログ、上部ステータス、マップ内テーマ表示など。
- `js/stage37-status-floor-items.js`
  - ステータス欄や足元アイテム表示まわりの補助。
- `js/enemy-catalog.js`
  - 敵定義。
- `js/spawns.js`
  - 階層別敵出現。
- `js/traps.js`
  - 罠効果。
- `js/trap-renderer.js`
  - 罠のCanvas表示。
- `js/audio.js` / `js/sound.js` / `js/sound-hooks.js`
  - BGM、SE、操作音・戦闘音の接続。
- `tests/`
  - Stage別smokeテストと個別機能テスト。

## 現在の主な仕様・実装状況

### ゲーム全体

- タイトルは「悠久の迷宮」。
- 起動時はプッシュスタートから冒険の書1～3を選び、新規時は王様とのオープニング、既存時は王城または迷宮途中から再開します。
- 冒険の書は `eternal_labyrinth_adventure_book_1` ～ `_3` に保存し、書式バージョンは1です。
- 王城では王様、迷宮選択、重要アイテムの宝箱、手動記録、タイトル帰還を選べます。
- 3種類の迷宮がある構成です。
  - ちょっと不思議系
  - 不思議系
  - もっと不思議系
- 「不思議」27Fの宝箱は道具袋を使わない重要アイテムで、取得後に上り階段で帰還します。王城で開封すると同じ入口から「もっと不思議」へ分岐します。
- 目的宝箱は通常アイテム抽選と分離しています。ちょっと不思議は10Fに必ず1個あり、取得前の10Fには利用可能な下り階段がありません。不思議／もっと不思議は未取得である限り27F以降の各階に必ず1個あります。3迷宮とも取得後は帰還中となり、上り階段で1Fから0Fへ出た時点でクリアします。
- 99F以降のループや深層向けの迷路型地形も導入済みです。
- セーブはブラウザの `localStorage` を使用します。

### UI

- PCとスマホで表示をかなり分けています。
- 王城は `treasureChest.obtained/opened` に応じて `画像/` 配下の3つのJPEG背景を切り替えます。王様会話、ダンジョン選択、宝箱確認、記録完了では同じ背景を4pxぼかし・暗転して前面パネルを表示します。
- ストーリー画面も現在の宝箱状態に合う王城背景を使用し、4pxぼかしと `rgba(0,4,9,.48)` の暗転上へ共通会話パネルを中央表示します。
- 縦長スマホではキャンペーン画面を `100dvh` とsafe-areaで配置し、王城背景を `100% auto / center top` で全幅表示します。王城メニューは背景画像の下側へ寄せ、王様・冒険者・宝箱台座を同時に見せます。PCは従来の `cover / center` です。
- タイトルのゲーム終了確認もタイトル迷宮背景を維持し、同じ4pxぼかしと暗転の上へ確認パネルを表示します。
- タイトルは `images/campaign/title-labyrinth-bg.jpeg`、冒険の書は `images/campaign/adventure-library-bg.jpeg` を使用します。文字・ボタン・セーブ情報は画像へ焼き込まずHTMLで生成します。
- タイトルと冒険の書は上下キー、Enter／Space、Esc、マウスホバー／クリック、スマホタップに対応し、選択中は金色の発光と小さな拡大で示します。
- ダンジョン画面上部の操作は「中断」に統一し、保存成功後は冒険の書選択へ戻ります。王城メニューの「タイトルへ戻る」は残しています。
- スマホでは操作UIをマップ直下へ集約する方向で調整中です。
- 8方向長押しのタイマーは、方向ボタンの `pointerup` / `pointercancel` / `pointerleave` に加え、documentの `pointercancel`、画面非表示、道具画面を開く時、道具を実行する時にも解除します。飛翔演出は `K.Game.isInputLocked()` 中には開始しません。
- 落とし穴と通常階段はどちらも `buildFloor()` 完了経路を使い、新階層生成前に敵ターンタイマー、方向長押し、ActionSequenceの飛翔待機、旧飛翔描画を消去します。スマホ操作の左列は足踏み・矢撃ち・ステータス・地図確認、右列は拾う・階段・向き変更・マップです（PCは従来配置）。
- BGM/SEの画面上ボタン・スライダーは不要として非表示化済みです。
- メッセージログはマップ内下部へ表示する形に変更済みです。
- ステータス欄はスマホで崩れやすいため、修正時は実機幅・CSS競合に注意してください。
- 詳細ステータスの「次のレベルまで」直上には「挑戦中の迷宮」を短縮名で表示し、目的宝箱取得後は迷宮名へ「（帰還中）」を付けます。

### マップ・視界

- 階層テーマにより壁・床・通路・装飾の見た目が切り替わります。
- ミニマップ、半透明全体マップ、マップのみ表示があります。
- `敵見えの紙片` と `道標の紙片` の効果は分離済みです。
  - 敵見え：敵の赤点のみ。
  - 道標：地形と階段のみ。敵・道具・罠は表示しない。
- `ワナ見えの紙片` は階全体の罠を識別し、`ワナ見え草` は20ターンの間、隣接8マスの罠だけを識別します。

### アイテム

- 道具袋は30枠です。矢は従来どおり1束99本までです。
- 1フロアの道具総数は45個までで、床道具・床ゴールド・道具袋・敵が盗んで保持している道具を数えます。ミミック相当の敵は現時点で未実装です。
- 新規フロアの通常道具は5～7個です。モンスターハウスは別枠で10～15個を生成しようとしますが、総数45個に達した時点で追加を止めます。
- 道具袋は拾った順を維持します。
- 自動ソートではなく、Rキー／整理ボタンで任意整理します。
- 床に置かれた対象選択型紙片も、道具袋から読む時と同じように対象選択画面を開きます。
- `食料の紙片` と `呪い消しの紙片` は対象選択式です。
- `杖なおしの紙片`、武器強化、盾強化は手持ち・装備中・足元を対象にできます。
- `木の矢`、`鉄の矢`、`貫通の矢` があり、矢は「撃つ」行動に統一されています。
- `貫通の矢` の強さは7です。
- 武器特効は敵の戦闘タグに一致した場合だけ最終ダメージを1.5倍し、複数タグ一致でも重複しません。獣狩り／魔断ち／竜殺しは5F／15F／25Fから出現します。
- 木の盾は基礎値1。毒よけの盾は毒そのものではなく、毒によるちから低下だけを防ぎます。毒よけの指輪は毒を完全に防ぎます。
- 鏡の盾は光弾系だけを50%で反射し、石の盾は自身への錆び・弱化を防ぎます。火よけの盾は防御後の炎ダメージを半減します。
- 透明の杖は直線上の敵を10ターン透明にし、水晶壁で反射した場合だけプレイヤー自身へ同じ効果が返ります。
- 矢や投げ物は床アイテムと重ならない落下判定を使います。

### 敵・戦闘

- 自然発生はターンを消費するプレイヤー行動50回ごとに1体、敵総数30体までです。上限時の発生機会は消費され、空きができても次の50行動までは連続発生しません。ダッシュは移動距離にかかわらず自然発生カウンターを1回だけ進めます。
- 敵は33種です。装備特効と盾耐性の選択肢を中層～深層で増やすため、どくヘビ、火のコウモリ、鉄こうら虫、まほうカラス、氷の鳥、岩ドラゴン、火炎ドラゴン、影ドラゴンを追加しています。
- 敵は基本的にプレイヤーへ追跡します。
- ふらふら移動は `まよい蛾` と `すりぬけ影` に限定する方針で調整済みです。
- 敵攻撃力は階層加算ではなく、敵キャラごとの固定値として強化済みです。
- `こんらん目玉` は25%の混乱にらみ相当へ変更済みです。
- 通路では非投射型特殊攻撃は隣接時、部屋では同じ部屋内で発動するよう調整済みです。
- `火ふき獣` などの遠距離炎は赤い球の投射演出があります。

### 状態異常・罠

- プレイヤー睡眠中は動けず、キャラアニメ停止と右上の `z` 表示があります。
- 状態異常が治った際はログ表示します。
- 罠は踏んだ後や見破った後に種類別表示へ変わります。
- 罠発動時は種類に応じたSE・演出を挟んでから効果が適用されます。
- `満腹の指輪` は `飢え苔の印` の満腹度減少を防ぎます。

### PC専用GMモード

- 通常プレイ中にPCの `Ctrl + Shift + G` を押した時だけ、画面右上へ `GM` ボタンが出ます。再入力で非表示になります。
- `hover: hover` かつ `pointer: fine` を満たさないスマホ・タブレット相当環境では、GMボタンとパネルをDOMへ生成せず、GM APIも入口を拒否します。
- GMパネルの1F／10F／27F／50F／99F移動は `K.Game.buildFloor()` を使い、通常のマップ・敵・道具・階段・目的宝箱・BGM生成を通します。迷宮の最大階を超える移動は拒否します。
- HP全回復、満腹度全回復、現在階の敵全消去も利用できます。
- GM開始直前に通常状態を保存し、その後の自動保存はGM専用キーへ分離します。冒険の書への直接保存もGM中は抑止します。
- GM表示・有効状態は永続化しません。旧モードキーも起動時に削除し、再読み込み後は必ず通常状態・GMボタン非表示から始まります。

### BGM・SE

- `BGM/悠久の迷宮（ゲームタイトル曲）.mp3` は、タイトルの「はじめる」をクリック・タップ・Enter／Spaceで決定したユーザー操作で再生を解禁し、冒険の書、オープニング、王城、ダンジョン選択まで継続再生します。
- ダンジョン開始時は既存の階層BGMへ切り替わり、王城へ帰還するとタイトルBGMへ戻ります。
- BGMは階層テーマに応じてMP3を切り替えます。
- SEは `BGM/SE` 配下のMP3を使用します。
- 画面の正式決定は `決定した音.mp3`、戻る・Esc・中止は `キャンセルする音.mp3`、迷宮突入は `階段を上り下りする音.mp3` を共通SE管理から再生します。
- 頻出SEは最初の `pointerdown` / `keydown` で共有AudioContextを `resume()` し、バックグラウンドでfetch・`decodeAudioData()`してAudioBufferを保持します。再生時は新しいAudioBufferSourceNodeから `start(0)` し、バッファ未準備・Web Audio失敗時だけ起動時に用意したHTMLAudioプールへフォールバックします。
- `K.Sound.timings()` で攻撃入力、共通SE呼び出し、Web Audioの `source.start(0)`（HTMLAudioフォールバック時は `playing`）の時刻を確認できます。通常はコンソール出力せず、`K.Sound.debugTimings(true)` を明示した時だけ `[SE DEBUG]` を出します。
- レベルアップSEは `BGM/レベルアップ.mp3` を使用します。
- スマホやブラウザではユーザー操作前に音が鳴らない制限があるため、音が出ない場合はオーディオ初期化・ユーザー操作起点の再生処理を確認してください。

## 直近で触っていた課題

直近では、王城・迷宮選択・中断画面と共通SEを調整しました。

- 宝箱未入手時の王城「宝箱を調べる」は無効表示で、マウス・タップ・キーボードから操作できません。入手済み状態で王城を描画すると即時有効になります。
- 「不思議」の解放条件は既存の `story.cleared.tutorialDungeon === true` のみです。新しい解放フラグはありません。
- 難易度表示はチュートリアル／通常が「普通」、もっと不思議が「難」です。内部値はそれぞれ `0.7 / 1 / 1.2` のままです。
- 中断確認中も `.game-shell` は背景として表示しますが、アプリ状態は `SUSPEND` のため `K.Game.isInputLocked()` が移動・攻撃・ターン進行を拒否します。
- 中断画面は現在のゲーム表示へ暗転・3pxぼかしをかけ、その上に金色枠の確認パネルを表示します。

以前のスマホ表示調整について：

- 文字重なりの原因：
  - 古いCSSの `grid-column` 指定や `nth-child` 系指定が残っていた。
  - ステータスDOMを行単位に組み直した後も、スマホ向けCSSが狭い列幅で上書きしていた。
- 対応済み：
  - ステータス欄を行単位の構造へ分離。
  - 古い直下グリッド指定の干渉を抑制。
  - CSS/JSのキャッシュ更新用バージョンを付与。
  - 1行目、攻防行、体力・満腹バーの配置を調整。
- 次に似た問題が起きた場合：
  - `style.css` 末尾のスマホ向け上書きを確認してください。
  - `js/stage37-status-floor-items.js` のステータスDOM再構成処理を確認してください。
  - ブラウザキャッシュの可能性も確認してください。

## 現在のTODO・既知の問題

- `README.md` は「Stage 1〜27」と書かれている一方、実際には `stage44` 付近までファイルがあります。
  - `TODO.md` に README 更新が登録されています。
- `KNOWN_ISSUES.md` 上では、確認済みの未解決不具合はありません。
- `enemy-catalog.js` には将来候補の敵能力TODOがあります。

## テスト方針

- 通常修正では、変更箇所に関係する短時間テストだけ実施してください。
- JavaScriptを変更した場合は、少なくとも構文エラー確認を行ってください。
- Nodeを使う場合は、環境によっては以下の bundled Node を使うと安定します。
  - `C:\Users\Kenji\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`
- 長時間テストや全Stageテストは、ユーザーが明示した場合だけで構いません。
- CSSやドキュメントだけの変更では、`git diff --check` 程度で十分な場合があります。

## Git運用

- ブランチ：`main`
- リモート：`origin https://github.com/GitStudio-Create/yuukyuu-no-meikyuu.git`
- ユーザーは、この試作品について確認なしのpushを許可しています。
- 作業後は原則として以下を行います。
  1. 変更内容を確認
  2. 必要に応じて短時間テスト
  3. `CHANGELOG.md` 更新
  4. commit
  5. push

## 新しいスレッド開始時のおすすめ依頼文

新しいCodexスレッドでは、最初に次のように依頼するとスムーズです。

```text
C:\Users\Kenji\Documents\Codex\悠久の迷宮 の開発を引き継いでください。
最初に AGENTS.md と HANDOFF.md を読んで、現在のコードを正として作業してください。
依頼範囲だけを最小限変更し、長時間テストや全Stageテストは明示するまで行わないでください。
このプロジェクトは試作品なので、通常の修正後は確認なしでcommit/pushして構いません。
```
