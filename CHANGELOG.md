# CHANGELOG

このファイルには、「悠久の迷宮」の主な変更履歴を記録します。

## [Unreleased]

### Added

- 開発管理用ドキュメントとして `CHANGELOG.md`、`TODO.md`、`KNOWN_ISSUES.md` を追加。

### Changed

- `AGENTS.md` にドキュメント更新ルールを追記。
- ゲーム表示タイトルを「悠久の迷宮」へ変更し、旧コンセプト由来の表示文言を整理。
- 旧コンセプト由来の紙片・武器・敵名を新名称へ整理。
- セーブ・GMモード・BGM/SE設定キーを新名称へ変更し、旧キーからの読み込み互換を追加。
- 差し替えBGMを正式BGMとして `BGM` 直下から読み込む構成へ変更。
- 木の矢を固定ダメージではなく、矢の強さ4とレベル別基本攻撃力から算出する攻撃力方式へ変更。
- `杖なおしの紙片` を使用する際、所持中の杖から修復対象を選べるように変更。
- 判明済みの罠を種類ごとの固有表示に変更し、未判明罠は共通表示のまま維持。
- `杖なおしの紙片`、武器強化、盾強化の対象選択を手持ち・装備中・足元へ拡張。
- 3F以降の対象部屋に、眠った敵・追加アイテム・罠を配置するモンスターハウスを追加。
- `敵見えの紙片` 使用後、ミニマップと全体マップへ現在フロアの敵位置を表示するよう修正。
- `道標の紙片` 使用後、現在フロアの罠をすべて種類判明済みにするよう修正。
- プレイヤー・敵のCanvas描画を8方向の向きが分かる表示にし、プレイヤーの武器・盾外見を装備状態に連動。
- `敵見えの紙片` と `道標の紙片` の表示効果を分離し、敵赤点・地形照明・罠道具表示が混同されないよう修正。
- 既存の半透明全体マップとは別に、ゲーム進行を止めて地図だけを確認できる「マップのみ表示」を追加。
- `食料の紙片` と `呪い消しの紙片` を対象選択式に変更し、上部のGMモードボタンは現在状態を維持してGMパネルを開く動作へ修正。
- 非移動行動後の敵ターン開始を0.5秒遅らせ、敵のGドロップ金額を全敵共通1～100Gへ統一し、ゲームオーバー時の所持金ランキングを追加。
- レベルアップ時のファンファーレをWebAudio生成音から `BGM/レベルアップ.mp3` の再生へ変更。
- 投擲、ワープ、弓、剣、素手、ダメージ、装備の主要SEを `BGM/SE` 配下のMP3へ差し替え。
- レベルアップ時のHP回復を全回復ではなく、最大HP増加分だけ現在HPへ加算する仕様に変更。
- `ワナ見えの紙片` 使用時、罠の位置だけでなく種類も即座に判明済みになるよう変更。
- `食料の紙片` の対象に `パン` と `くさったパン` を含め、`大きなパン` へ変えられるよう変更。
- `道具見えの紙片` 使用後、未探索エリアにある床道具もミニマップ・全体マップへ表示されるよう修正。
- プレイヤー睡眠中はキャラの待機・歩行アニメを停止し、右上に `z` 表示を出すよう修正。
- `ワナ見え草` 使用時も紙片と同じくフロア内の罠を位置・種類判明済みにし、未探索エリア上の罠もマップ表示されるよう修正。
- `満腹の指輪` 装備中は `飢え苔の印` の満腹度減少を防ぐよう修正。
- チュートリアル系ダンジョンの上限階を越えた場合、通常ダンジョン相当の階層別敵候補を使い、11F以降に1F帯の敵が戻らないよう修正。
- 床に置かれた対象選択型の紙片を読む場合も、道具袋から読む時と同じ対象選択画面を開くよう修正。
- ステータス欄の1行目を4項目の均等幅にし、剣・盾表示の間隔と体力・満腹バーの高さ・長さを調整。
- `力の指輪` 装備中、攻撃力だけでなく表示上の `ちから` も補正込みで表示されるよう修正。
- `火ふき草` を、飲む場合は65～75ダメージと満腹度1回復、投げた場合は35～40ダメージに変更し、炎で倒した敵の足元の道具が燃えるよう修正。
- メニューを開く・決定・キャンセルの操作SEを `BGM/SE` 配下のMP3へ割り当て。
- 決定SEを確認実行・選択決定時へ整理し、カーソル移動では鳴らさず音量も控えめに調整。
- 半透明の背景マップはEscで閉じずMキーのみで切り替えるようにし、詳細ステータスを閉じる時はキャンセルSEを鳴らすよう修正。
- 装備中の矢の行動を「投げる」ではなく「撃つ」に統一し、通常矢として強力な `鉄の矢` を追加。決定SEの音量も少し上げた。
- 階段を上り下りする際のSEを `BGM/SE/階段を上り下りする音.mp3` へ割り当て。
- 決定SEの音量補正を解除し、床アイテム使用などの汎用道具使用SEも `決定した音.mp3` へ統一。
- 敵攻撃力を階層加算ではなく敵キャラごとの固定値として強化し、ふらふら移動の敵を `まよい蛾` と `すりぬけ影` に限定。`こんらん目玉` は25%の混乱にらみへ変更し、目つぶし・透明系の草と杖を追加。
- `火ふき獣` など敵の遠距離炎に赤い球の投射演出を追加し、プレイヤーの睡眠・混乱・目つぶし・透明・倍速が切れた時にログを表示。
- 壁に付く `水晶壁` を追加し、杖の光弾が当たると効果がプレイヤーへ跳ね返るように変更。
- タイトル画面と上部操作に3種類の迷宮選択を追加し、宝箱取得後は上り階段で地上へ帰還する進行を追加。
- `迷路型` の地形タイプを追加し、50F以降の10の倍数階と99Fで出やすく、序盤からも低確率で出るように変更。
- ゲーム画面左側に残っていた迷宮選択パネルを削除し、迷宮選択は上部ボタンに統一。
- 睡眠中は矢の発射アニメーションも含めて撃てないようにし、99本の矢も撃った直後に本数表示が減るよう修正。
- 道具袋は拾った順を維持し、自動ソートではなくRキー／整理ボタンで任意に整理する仕様へ変更。整理順は武器・盾・矢・指輪・食料・草・紙片・杖を基本にした。
- `力もどし草` を飲んだ時も、他の火ふき草以外の草と同じく満腹度が5回復するよう修正。
- `空洞床` で下の階へ落ちる時、短い落下演出とSEを挟んでから階層を切り替えるよう修正。
- 装備中の矢を撃った場合も共通の落下判定を通し、床道具と重ならず、落下不可なら消滅ログを出すよう修正。
- 罠発動時、罠ごとのSEと短い発動演出を挟んでからプレイヤーへ効果を適用するよう変更。
- 透明状態を敵視界判定へ反映し、透明草・透明の杖の効果中は敵に居場所を見つからないよう修正。草の使用ログと一部効果説明の不一致も整理。
- プレイヤー歩行時の足フレーム振り幅を抑え、スライド移動中にブルブルして見える挙動を軽減。
- プレイヤー描画の下方向移動を落下演出時だけに限定し、横移動中に下のマスへはみ出して見える挙動を修正。
- 敵の非投射型特殊攻撃、草によるHP回復・最大HP上昇、ちから回復、毒、落とし穴に専用SEを割り当て。
- にらみ・杖術など非投射型の敵特殊攻撃は、同じ部屋内または隣接時のみ発動するよう修正。
- 現行マップ生成の部屋サイズに合わせて、モンスターハウス候補部屋の条件を緩和し通常生成で出現しやすく修正。
- マップ上の罠表示を、道具の水色丸と被りにくい菱形・三角・十字などの形状へ変更。
- セーブ復元後も装備中の矢を道具袋内の同一スタックへ結び直し、99本の矢を撃っても道具袋表示が減らない不具合を修正。
- 視界外の通常敵もランダム移動せずプレイヤー方向へ追跡し、ふらふら移動は `まよい蛾` と `すりぬけ影` に限定されるよう修正。
- 追跡敵は斜め移動可能な場合に斜めを優先して接近するよう修正し、識別済みアイテムは同じ冒険中の同種アイテムにも正式名表示を引き継ぐよう変更。
- スマホ表示でタイトル・ステータス・マップ領域が崩れないようレスポンシブCSSを調整し、PC表示と同じ情報量を保つよう修正。
- 上部の不要な迷宮選択・GM起動ボタンとBGM/SEのONボタンを非表示化し、音量スライダー中心の音声UIへ整理。スマホでは移動操作を道具袋より上に配置して攻撃ボタンを足元操作列へ追加。
- PC・スマホのBGM/SE操作UIを画面から除外し、スマホでは8方向操作をマップ直下へ移動。矢撃ち・向き変更・走る・ステータスを足元操作列へ集約し、ステータス文字の重なりを軽減。
- `中断` を `最初から` の横へ移動し、マップ上部の地図ボタンと音声バーを非表示化。操作UIはマップ直下に集約し、矢撃ち・斜め移動・地図確認などの補助操作も同じ操作エリアへ整理。
- ステータス欄を圧縮し、階層テーマ・所持金表示を外して次レベル表示を見やすくし、ログ欄をマップ内下部へ移動。方向ボタン長押しで約0.3秒間隔の連続移動にも対応。
- スマホのステータス2行目が重ならないよう4列配置へ調整し、攻撃力・防御力を左右並びに変更。操作ボタンから重複する攻撃・斜め移動・走るを外し、ログ表示を少し小さく調整。
- スマホステータス欄で古い `grid-column` 指定が残り、ちから・レベル・次レベル・攻防表示が重なる問題を、各項目の行列を明示する形で修正。
- ステータス欄を行単位の構造へ分け、過去の直下グリッド指定や `nth-child` 指定が能力値表示へ干渉しないよう修正。
- ステータス欄の表示順を、階層・ちから・レベル・次レベル、攻防、体力・満腹、各バーの順に変更。
- スマホ側が古いステータスDOM/CSSを保持した場合でも、描画時に指定順へ組み直す保険処理を追加し、CSS/該当JSの読み込みにキャッシュ更新用バージョンを付与。
- スマホの上段・攻防・体力／満腹の行間指定を互いに独立させ、体力／満腹の文字とバーの重なりを解消してバー幅を3倍に拡張。
- PC・スマホ共通のステータス欄を行単位のCSS Gridへ整理し、旧配置指定をリセット。体力・満腹のラベル、バー、数値を別段にして、画面幅による重なりや省略表示を解消。

## [既存実装]

### Added

- `index.html` を開くだけで遊べる、外部ライブラリなしの静的なターン制ローグライクとして実装済み。
- `js/dungeons.js` で、10Fのチュートリアルダンジョン、30Fの通常ダンジョン、99Fの未識別ダンジョンを定義済み。
- `README.md` で、Stage 18からStage 27までの主要実装内容を記録済み。
- `index.html` で、Stage 40までのStage別JavaScriptファイルを読み込み済み。
- `tests/` に、Stage 5からStage 40までの代表的なsmokeテストと、マップ生成確認用テストを配置済み。
- `tests/stage39-corridor-stress.js` に、地形生成の長時間ストレステストを配置済み。

### Changed

- `README.md` のコード構成には、状態管理、ダンジョン定義、マップ生成、視界、戦闘、道具、UI、入力、音声、スプライト、ゲーム進行の主要モジュールが整理済み。
