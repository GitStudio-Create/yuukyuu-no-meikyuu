# 悠久の迷宮 開発ルール

## 基本方針

- 依頼された範囲だけを変更する。
- 関係のない機能、表示、数値、ファイルは変更しない。
- 既存仕様を推測で作り直さない。
- 大規模なリファクタリングは、明示的に依頼された場合のみ行う。
- 変更量は必要最小限にする。
- 既存のゲーム進行、セーブデータ、PC操作、スマホ操作を壊さない。

## 通常時のテスト方針

- 通常の修正では、変更箇所と直接影響する範囲だけをテストする。
- 毎回の全Stage回帰テストは行わない。
- 毎回の長時間地形ストレステストは行わない。
- 毎回の全アイテム、全敵、全階層の確認は行わない。
- 120秒以上かかるテストは、原則として自動実行しない。
- テストが長時間化しそうな場合は、実行前に理由と予想時間を報告する。
- 同じテストを必要なく繰り返さない。
- 変更と無関係な既知の失敗は、今回の修正対象に含めない。

## 全体テストを行う条件

以下のいずれかの場合のみ、全体回帰テストを行う。

1. ユーザーが「全体回帰テスト」と明示した場合
2. マップ生成、ターン処理、セーブ形式など、ゲーム全体へ影響する基盤部分を変更した場合
3. 複数機能をまとめた節目の確認を行う場合

全体回帰テストを行う場合も、最初に短い代表テストを実行し、
問題がなければ段階的に範囲を広げる。

## テストの優先順位

1. 構文エラーと読み込みエラー
2. 今回変更した機能
3. 直接影響する周辺機能
4. 必要な場合のみ代表的な回帰テスト
5. ユーザーから指示された場合のみ全体回帰テスト

## 作業報告

作業完了時は、以下だけを簡潔に報告する。

- 変更した内容
- 変更したファイル
- 実行したテスト
- テスト結果
- 未確認事項または残っている問題

不要な長文説明や、依頼と関係のない追加提案は行わない。

## 実装前の確認

依頼内容が明確で、既存コードから仕様を判断できる場合は、
質問せずに実装を進める。

仕様が複数の意味に取れ、誤実装の可能性が高い場合だけ、
実装前に短く確認する。

## ファイル調査

- 最初から全ファイルを読み直さない。
- 関連するファイルを検索してから必要部分だけ読む。
- すでに確認済みの内容を、同じタスク内で何度も調べ直さない。
- 変更対象と無関係な大容量ファイルや生成物は調査しない。

## 禁止事項

- 依頼されていない仕様変更
- 依頼されていない名称変更
- 依頼されていない数値調整
- 既存デザインの全面変更
- 毎回の長時間ストレステスト
- テストを通すためだけのゲーム仕様変更
- テスト側を安易に正解へ合わせる変更

## ドキュメント更新ルール

- 機能追加、仕様変更、不具合修正を行った場合は、必要に応じて `CHANGELOG.md` を更新する。
- 新しい未実装項目や改善予定が確定した場合は `TODO.md` へ追加する。
- TODOを完了した場合は、`TODO.md` から完了項目を削除するか、完了済みとして整理する。
- 未解決の不具合を確認した場合は `KNOWN_ISSUES.md` へ追加する。
- 不具合を修正した場合は `KNOWN_ISSUES.md` から削除し、`CHANGELOG.md` へ修正内容を記録する。
- 推測や未確認情報を確定事項として記録しない。
- 軽微な文言変更など、履歴として価値が低い変更まで必ず記録する必要はない。
- ドキュメント更新のためだけにゲームコードを変更しない。
